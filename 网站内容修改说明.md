# JSY Crystal 网站内容修改说明

以后主要修改这个文件：

```text
content/site-content.json
```

## 1. 修改联系方式

打开 `content/site-content.json`，找到：

```json
"email": "marketing@jsycrystal.com",
"website": "jsycrystal.com"
```

改成新的邮箱或域名即可。

## 2. 修改首页标题和介绍

找到：

```json
"home": {
  "title": "...",
  "eyebrow": "...",
  "intro": "..."
}
```

- `title` 是首页最大标题
- `eyebrow` 是标题上方的小字
- `intro` 是首页介绍段落

## 3. 修改产品分类

找到：

```json
"products": [...]
```

每个产品包含：

- `name`：产品名称
- `label`：产品标签
- `url`：点击后进入的页面路径
- `description`：产品说明

如果只是改产品文案，改 `name`、`label`、`description` 即可。不要随便改 `url`，除非你也新建了对应页面。

## 4. 把内容同步到网页

修改 `content/site-content.json` 后，在网站文件夹运行：

```powershell
node scripts/apply-content.mjs
```

然后重新发布：

```powershell
npx.cmd vercel --prod
```

## 5. 换图片

图片放在：

```text
assets
```

目前主要图片：

```text
assets/linkedin-crystal-bracelet-money-tree-geode.png
assets/linkedin-crystal-b2b-banner.png
assets/jsy-logo.svg
```

如果你要换图，最简单是用同样文件名覆盖原图，然后重新发布。

## 6. 增加新的产品页面

现在已有页面：

```text
products/
products/bracelets/
products/bracelets/materials/
products/bracelets/materials/amethyst/
products/bangles/
products/rings-necklaces-pendants/
products/decorative-crystals/
```

如果要新增页面，建议先复制一个已有分类文件夹，再改里面的 `index.html`。

## 7. 注意事项

- JSON 文件里的英文双引号不能删。
- 每一行内容后面的逗号不要随便删。
- 如果运行同步脚本报错，通常是 JSON 格式写错了，可以把截图发给 Codex 检查。
